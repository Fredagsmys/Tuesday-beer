<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Timer" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="startButton">@style/startButton</item>
        <item name="stopButton">@style/startButton</item>
        <item name="varvButton">@style/startButton</item>
        <item name="resumeButton">@style/startButton</item>
        <item name="resetButton">@style/startButton</item>

        <item name="android:textViewStyle">@style/textViewStyle</item>
        <!-- Primary brand color. -->
<!--        <item name="colorPrimary">@color/black</item>-->
        <item name="colorPrimaryVariant">@color/black</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorPrimary">@color/gray</item>
        <item name="colorPrimaryDark">@color/gray</item>
        <item name="colorAccent">@color/colorAccent</item>

    </style>
    <style name="textViewStyle" parent="android:Widget.TextView">
        <item name="android:fontFamily">monospace</item>
    </style>

    <style name="MyButton" parent="Widget.AppCompat.Button">
        <item name="android:textSize">13dp</item>
        <item name="android:textColor">@color/white</item>

        <item name="android:paddingLeft">20dp</item>
        <item name="android:paddingRight">20dp</item>
    </style>
    <style name="startButton" parent="MyButton">
        <item name="android:background">@drawable/ripple_button</item>
    </style>

    <style name="stopButton" parent="MyButton">
        <item name="android:background">@drawable/ripple_button</item>
    </style>

    <style name="varvButton" parent="MyButton">
        <item name="android:background">@drawable/ripple_button</item>
    </style>

    <style name="resumeButton" parent="MyButton">
        <item name="android:background">@drawable/ripple_button</item>
    </style>

</resources>